const { request, response } = require("express");
const TiquetesModelo = require("../modelos/ClienteModelo");
const TiqueteOperaciones = {};

TiqueteOperaciones.crearTiquetes = async(request, response) => {

}
TiqueteOperaciones.consultarTiquetes = async(request, response) => {
    try {
        const listatiquetes = await TiquetesModelo.find();
        response.status(200).send(listatiquetes);
    } catch (error) {
        res.status(400).send("Mala peticion");
    }
}

module.exports = TiqueteOperaciones;